## Pre School [Svelte version]

A preschool application built with `svelte` as a learning tool.

### Build

To setup the dependencies just run:

`npm install`

To build application run:

`npm run build`
